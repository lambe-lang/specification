type Either a b =
  data Left  { value : a }
| data Right { value : b }

impl for Either a b {
    sig fold : self -> (Left a -> c) -> (Right b -> c) -> c

    def Left.fold  f _ = f self
    def Right.fold _ f = f self
}

