import char

kind int : type

impl int {
    sig (--) : self -> self
    sig (++) : self -> self
    sig (+)  : self -> self -> self // native
    sig (-)  : self -> self -> self // native
    sig abs  : self -> self         // native
    sig toChar : self -> Char       // native

    def (--) = self - 1
    def (++) = self + 1

    // TODO ...
}
