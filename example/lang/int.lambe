impl char

kind Int : type

impl for Int {
    sig (--) : self -> self
    sig (++) : self -> self
    sig (+)  : self -> self -> self // native
    sig (-)  : self -> self -> self // native
    sig abs  : self -> self         // native
    sig toChar : self -> Char

    def (--) = self - 1
    def (++) = self + 1

    // TODO ...
}
