import int
import char

data string

impl string {
    sig length : self -> int
    sig chatAt : self -> int -> Option char
    sig (+) : self -> self -> self
    sig replace : self -> char -> char -> self
    sig index : self -> char -> int
}
