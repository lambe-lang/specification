trait Traversable (t:type -> type) (f:type -> type) with Applicative f {
    sig traverse : forall a b. self -> t a -> f (t b) for (a -> f b)
    sig sequence : forall a b. self -> f (t a) for t (f a)

    def traverse = self map |> sequenceA
    def sequence = id traverse
}

