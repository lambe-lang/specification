module concurrent.promise.data

data Future : type -> type

trait AsyncRunner {
    def async : [a] (unit -> a) -> Future a
}

defined AsyncRunner {
    def async f = native1 "concurrent.async" f
}

trait Promise with Monad Future
