module lang.data.core

from lang.data.boolean import (Bool)

trait Adder (a:type) {
    def (+) : a -> a
}

trait Arithmetic (a) for a {
    def (-) : a -> a
    def (*) : a -> a
    def (/) : a -> a
}

trait Equatable (a) for a {
    def (==) : a -> Bool
}

